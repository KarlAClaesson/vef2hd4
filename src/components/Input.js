const Input = (label) => {
    return(
        <>
            <label className="field_label">{label}</label>
            <input className="field input"></input>
        </>
    )
}

export default Input